/**
 * Stra188 fetch endpoints – each is separate. Add/remove in endpoints/index.ts to enable/disable.
 */
export type { FetchEndpointDef } from './endpoints/types';
export { FETCH_ENDPOINTS } from './endpoints';
